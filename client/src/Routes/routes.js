const API = 'http://127.0.0.1:5000/api';
const authentication = 'authentication';
const questions = 'questions';
const profile = 'profile';
const answers = `${API}/answers`;
export const registration = `${API}/${authentication}/registration`;
export const login = `${API}/${authentication}/login`;
export const deleteUser = `${API}/${authentication}/delete`;
export const like = `${API}/${questions}/likeQuestion`;
export const dislike = `${API}/${questions}/dislikeQuestion`;
export const addNew = `${API}/${questions}/addNew`;
export const viewQuestions = `${API}/${questions}/`;
export const myQuestions = `${API}/${questions}/myQuestions`;
export const viewProfile = `${API}/${profile}/view`;
export const editProfile = `${API}/${profile}/editProfile`;
export const changePassword = `${API}/${profile}/change-password`;
export const viewQuestion = `${API}/${questions}/viewQuestion`;
export const viewAnswers = answers;
export const addAnswer = `${answers}/addNew`;